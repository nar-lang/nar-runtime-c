#include "runtime.h"
#include "include/nar-runtime.h"

const nar_t package_pointers = {
        .alloc = &nar_alloc,
        .realloc = &nar_realloc,
        .free = &nar_free,
        .frame_alloc = &nar_frame_alloc,
        .register_def = &nar_register_def,
        .apply = &nar_apply,
        .apply_func = &nar_apply_func,
        .print = &nar_print,
        .fail = &nar_fail,
        .get_last_error = &nar_get_last_error,
        .object_get_kind = &nar_object_get_kind,
        .object_is_valid = &nar_object_is_valid,
        .new_unit = &nar_new_unit,
        .to_unit = &nar_to_unit,
        .new_char = &nar_new_char,
        .to_char = &nar_to_char,
        .new_int = &nar_new_int,
        .to_int = &nar_to_int,
        .new_float = &nar_new_float,
        .to_float = &nar_to_float,
        .new_string = &nar_new_string,
        .to_string = &nar_to_string,
        .new_record = &nar_new_record,
        .new_record_field = &nar_new_record_field,
        .new_record_field_obj = &nar_new_record_field_obj,
        .new_record_raw = &nar_new_record_raw,
        .to_record = &nar_to_record,
        .to_record_field = &nar_to_record_field,
        .to_record_item = &nar_to_record_item,
        .new_list_cons = &nar_new_list_cons,
        .new_list = &nar_new_list,
        .to_list = &nar_to_list,
        .to_list_item = &nar_to_list_item,
        .new_tuple = &nar_new_tuple,
        .to_tuple = &nar_to_tuple,
        .to_tuple_item = &nar_to_tuple_item,
        .new_option = &nar_new_option,
        .to_option = &nar_to_option,
        .to_option_item = &nar_to_option_item,
        .new_bool = &nar_new_bool,
        .to_bool = &nar_to_bool,
        .new_func = &nar_new_func,
        .to_func = &nar_to_func,
        .new_native = &nar_new_native,
        .to_native = &nar_to_native,
        .new_closure = &nar_new_closure,
        .to_closure = &nar_to_closure,
};
